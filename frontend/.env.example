# React 앱 환경 변수 예시
# 이 파일을 'frontend/.env'로 복사해서 사용하세요.
# (.gitignore에 의해 frontend/.env 파일은 Git에 올라가지 않습니다)
#
# 이 포트 번호는 프로젝트 최상위 .env 파일의 'HOST_BACKEND_PORT' 값과
# 반드시 일치해야 합니다. (React 앱이 API를 호출할 백엔드 포트)
REACT_APP_BACKEND_PORT=8800