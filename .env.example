# CPU 이상 탐지 대시보드 - 환경 변수 설정 예시
# ---------------------------------------------------
# 새로운 개발자는 이 파일을 '.env' (Git 무시 파일)로 복사한 뒤,
# 'changeme'와 같이 비어있는 실제 값(예: 비밀번호)을 채워야 합니다.
# ---------------------------------------------------

# 1. PostgreSQL DB 설정 ('db' 서비스용)
# (주의! 실제 운영(배포) 시에는 'changeme' 비밀번호를 꼭 변경하세요!)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=postgres

# 2. 백엔드 (FastAPI) 설정
# 이 변수들은 'backend' 서비스 컨테이너로 전달됩니다.
ENV=development
# CORS 허용할 출처 (React 앱 주소)
# docker-compose.yml의 HOST_FRONTEND_PORT와 맞춰야 합니다.
ORIGINS=["http://localhost:3300","http://127.0.0.1:3300"]
# 'db'는 docker-compose.yml에 정의된 'db' 서비스의 이름입니다.
DB_HOST=db
DB_PORT=5432
# 로컬 PC에서 백엔드 API에 접속할 포트 (FastAPI)
HOST_BACKEND_PORT=8800

# 3. 프론트엔드 (React) 설정
# 로컬 PC에서 React 개발 서버에 접속할 포트
HOST_FRONTEND_PORT=3300