version: "3.9" # 최신 Compose Spec 버전 사용

services:
  db:
    image: postgres:16-alpine # PostgreSQL 16 버전으로 업데이트
    container_name: fraud-detection-db
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${DB_PORT}:5432"
    restart: unless-stopped

  backend:
    build: ./backend
    container_name: fraud-detection-backend
    # app/main.py 실행, /backend/app 폴더 감시
    command: uvicorn app.main:app --host 0.0.0.0 --port 8800 --reload --reload-dir /backend/app
    volumes:
      # 1. 소스코드 마운트
      - ./backend:/backend
      # 2. 모델 파일과 스케일러를 컨테이너 내부로 마운트 (프로젝트 루트에 위치해야 함)
      - ./isolation_forest_model.pkl:/backend/model/isolation_forest_model.pkl
      - ./amount_time_scaler.pkl:/backend/model/amount_time_scaler.pkl
    ports:
      - "${HOST_BACKEND_PORT}:8800"
    environment:
      - ENV=${ENV}
      - ORIGINS=${ORIGINS}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      # DB 접속 URL 완성
      - DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}
      # 모델 로드 경로 설정
      - MODEL_PATH=/backend/model/isolation_forest_model.pkl
      - SCALER_PATH=/backend/model/amount_time_scaler.pkl
    depends_on:
      - db
    restart: unless-stopped

  frontend:
    # Dockerfile을 수정하여 node:20-alpine을 사용합니다.
    build: ./frontend
    container_name: fraud-detection-frontend
    volumes:
      - ./frontend:/frontend
      - /frontend/node_modules
    ports:
      - "${HOST_FRONTEND_PORT}:3300"
    environment:
      - CHOKIDAR_USEPOLLING=true
      - PORT=3300
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
